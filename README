这个是一项目的基础框架，包含了网络请求框架，MVP框架，列表页，刷新页，以及列表跟刷新页的无数据网络请求失败的状态显示
主要的使用方式写在sample模块中，具体使用时可以将这个模块删除
base模块中有常用工具类，可以自行使用，扩展
网络框架及MVP的使用一定要遵循规范继承各自的base类
一些三方库的地址都写在了libs_version中也可以自行扩展
新建模块的版本号一定要跟sample中的一样
如果要使用ARouter实现路由跳转界面，请在各自的子模块的build.gradle中加入annotationProcessor 'com.alibaba:arouter-compiler:1.1.4' 后面1.1.4是版本号，不然会跳转失败